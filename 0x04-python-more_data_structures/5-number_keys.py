#!/usr/bin/python3

def number_keys(a_dictionary):
    """
    Gives the number of keys a dictionary has

    Args:
        @a_dictionary: the dictionary

    Return:
        The total number of keys
    """
    return (len(list(a_dictionary)))
